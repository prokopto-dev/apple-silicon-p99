diff --git a/Source/Windows/UnixLib/FEXUnixLib.cpp b/Source/Windows/UnixLib/FEXUnixLib.cpp
index 692e459..bd3d589 100644
--- a/Source/Windows/UnixLib/FEXUnixLib.cpp
+++ b/Source/Windows/UnixLib/FEXUnixLib.cpp
@@ -4,7 +4,9 @@
 #include <fcntl.h>
 #include <string>
 #include <sys/mman.h>
+#ifndef __APPLE__
 #include <sys/prctl.h>
+#endif
 #include <unistd.h>
 
 #include "FEXUnixLib.h"
@@ -45,6 +47,7 @@ constexpr NTSTATUS STATUS_INTERNAL_ERROR = 0xC00000E5;
 #define PR_SET_VMA_ANON_NAME 0
 #endif
 
+#ifndef __APPLE__
 static NTSTATUS FEXUnixLib_SetHardwareTSOControlHandler(void* _Args) {
   auto Args = reinterpret_cast<const FEXUnixLib_SetHardwareTSOControlArgs*>(_Args);
   if (Args->Enable) {
@@ -63,6 +66,17 @@ static NTSTATUS FEXUnixLib_SetKernelUnalignedAtomicControlHandler(void* _Args) {
   auto Args = reinterpret_cast<const FEXUnixLib_SetKernelUnalignedAtomicControl*>(_Args);
   return prctl(PR_ARM64_SET_UNALIGN_ATOMIC, Args->Flags, 0, 0, 0) == 0 ? STATUS_SUCCESS : STATUS_NOT_SUPPORTED;
 }
+#else
+// Darwin has no Linux prctl memory-model or unaligned-atomic controls. FEX's
+// portable software paths remain responsible for these semantics.
+static NTSTATUS FEXUnixLib_SetHardwareTSOControlHandler(void*) {
+  return STATUS_NOT_SUPPORTED;
+}
+
+static NTSTATUS FEXUnixLib_SetKernelUnalignedAtomicControlHandler(void*) {
+  return STATUS_NOT_SUPPORTED;
+}
+#endif
 
 static NTSTATUS FEXUnixLib_MadviseHandler(void* _Args) {
   auto Args = reinterpret_cast<const FEXUnixLib_Madvise*>(_Args);
@@ -71,22 +85,42 @@ static NTSTATUS FEXUnixLib_MadviseHandler(void* _Args) {
 
 static NTSTATUS FEXUnixLib_SetVMANameHandler(void* _Args) {
   auto Args = reinterpret_cast<const FEXUnixLib_SetVMAName*>(_Args);
+#ifdef __APPLE__
+  (void)Args;
+  return STATUS_NOT_SUPPORTED;
+#else
   return prctl(PR_SET_VMA, PR_SET_VMA_ANON_NAME, reinterpret_cast<unsigned long>(Args->Addr), Args->Size, Args->Name) == 0 ?
            STATUS_SUCCESS :
            STATUS_INTERNAL_ERROR;
+#endif
 }
 
 constexpr static int USER_PERMS = S_IRWXU | S_IRWXG | S_IRWXO;
+static std::string GetStatsName() {
+#ifdef __APPLE__
+  // Darwin enforces POSIX's leading-slash requirement for shm_open names.
+  return "/fex-" + std::to_string(getpid()) + "-stats";
+#else
+  return "fex-" + std::to_string(getpid()) + "-stats";
+#endif
+}
+
 static inline void* InitializeSHM(uint32_t MapSize, uint32_t MaxSize) {
   void* Base {};
-  const std::string Name = "fex-" + std::to_string(getpid()) + "-stats";
+  const std::string Name = GetStatsName();
   int fd = shm_open(Name.c_str(), O_CREAT | O_TRUNC | O_RDWR, USER_PERMS);
 
   if (fd == -1) {
     return nullptr;
   }
 
+#ifdef __APPLE__
+  // Darwin rejects growing this object after it has an active shared mapping.
+  // Size the backing object to the already-known reservation maximum now.
+  if (ftruncate(fd, MaxSize) == -1) {
+#else
   if (ftruncate(fd, MapSize) == -1) {
+#endif
     goto err;
   }
 
@@ -101,7 +135,9 @@ static inline void* InitializeSHM(uint32_t MapSize, uint32_t MaxSize) {
     goto err;
   }
 
+#ifndef __APPLE__
   prctl(PR_SET_VMA, PR_SET_VMA_ANON_NAME, reinterpret_cast<void*>(Base), MaxSize, "FEXMem_Misc");
+#endif
 
   // Allocate a small working shared space for now, grow as necessary.
   {
@@ -121,7 +157,7 @@ err:
 
 static inline bool AllocateMoreSlots(void* SHMBase, uint32_t NewSize) {
   bool Result {};
-  const std::string Name = "fex-" + std::to_string(getpid()) + "-stats";
+  const std::string Name = GetStatsName();
 
   // When allocating more slots, open the fd without O_TRUNC | O_CREAT.
   int fd = shm_open(Name.c_str(), O_RDWR, USER_PERMS);
@@ -129,9 +165,11 @@ static inline bool AllocateMoreSlots(void* SHMBase, uint32_t NewSize) {
     return false;
   }
 
+#ifndef __APPLE__
   if (ftruncate(fd, NewSize) == -1) {
     goto err;
   }
+#endif
 
   {
     auto SharedBase = mmap(SHMBase, NewSize, PROT_READ | PROT_WRITE, MAP_SHARED | MAP_FIXED, fd, 0);
@@ -158,7 +196,7 @@ static NTSTATUS FEXUnixLib_GetSHMStatsVMAHandler(void* _Args) {
 }
 
 static NTSTATUS FEXUnixLib_DeleteSHMStatsFileHandler(void* _Args) {
-  const std::string Name = "fex-" + std::to_string(getpid()) + "-stats";
+  const std::string Name = GetStatsName();
   shm_unlink(Name.c_str());
   return STATUS_SUCCESS;
 }
